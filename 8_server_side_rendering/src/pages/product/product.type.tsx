export type ProductType = {
  id: number;
  name: string;
  price: number;
  size: string;
  category: string;
  image: string;
};

/**
 * type untuk props dan product
 * digunakan untuk mendeklarasikan type
 */
